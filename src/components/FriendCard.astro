---
const { name, url, avatar, desc } = Astro.props;
---

<a class="friend-card" href={url}>
  <img src={avatar} alt="avatar" />
  <div class="ml-4">
    <div class="name">{name}</div>
    <div class="text-sm">{desc}</div>
  </div>
</a>
