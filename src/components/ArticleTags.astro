---
interface Props {
  tags?: string[];
}
const { tags } = Astro.props;
---

{
  tags && (
    <div class="article-tags">
      {tags.map((tag) => (
        <a class="article-tag" href={`/tags/${tag}`}>
          #{tag}
        </a>
      ))}
    </div>
  )
}
